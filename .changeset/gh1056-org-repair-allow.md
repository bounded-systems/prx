---
---

No release: carry the canonical org `permissions.allow` entry ("Bash(bash .claude/org-repair.sh)") in `buildOrgHarnessSettings()` and regenerate the checked-in `.claude/settings.json` (GH-1056, .github-private#491). `@bounded-systems/prx` is private/unpublished, so this is a no-version-bump change per the changeset gate's own guidance.
