---
---

feat: add the spec-driven `prx docs` verb (render + check over jsonld/readme/
community/cli/claude-context), and de-eager the doc-build modules to lazy
getRepoRoot so they're safe in the compiled binary. No package version change.
