---
---

Add the `seam` keyword to the capability-seam packages (`proc`, `env`, `gh`,
`git`, `cas`; `fs` already had it) so the bounded.tools site generator's
`prxSeams()` keyword filter reconciles to the live seam set. Metadata only: no
API or behavior change, no package release.
