---
---

internal cleanup: retire the 5 gen-*-feature scripts now that `prx features`
renders them; rewrite the feature banners + test hints to cite the verb
(features:render). No package version change.
