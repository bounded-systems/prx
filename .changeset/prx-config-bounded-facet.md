---
"@bounded-systems/prx-config": patch
---

Author `bounded.{facet,role,domain}` (noun / config-schema / tui-config) so prx-config is a labeled node in the @bounded-systems registry knowledge graph. Metadata only.
