---
"@bounded-systems/prx": patch
---

beadsd-box: chmod 700 the box-local `.beads` (the copy from `/work` inherited
0755, which bd warns about). Re-pinned `BEADSD_ROOM_IMAGE`. Cosmetic — drops the
startup permissions warning.
