---
---

tests only: cover three already-pure / already-injectable prx modules to 100%
line coverage — `claude/partial_capture` (cancellation draft sink),
`pr-state/repo_locate` (slug-or-cwd locator), and
`transcripts-digest/sources/registry` (source-adapter registry). No package
change.
