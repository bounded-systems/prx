---
---

internal: raise three 80–85-band source files past the per-file floor —
`plan-show-verb` (28.4% → 95.2%, drives the show/paths modes + not-found
rethrow on a new VerbSpec from the cli decomposition), `triage/promote`
(81.4% → 95.4%, covers the `runPromoteActor` wrapper + the priority::none
refusal), and `pr-state/next_work` (83.5% → 95.9%, covers the prx.toml
`[next_work]` config readers + `priorityLabelToNumber`). Test-only.
