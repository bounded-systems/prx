---
---

internal: add ts-morph + a reusable extract-module codemod, and start decomposing
pr-state/cli.ts — extract CliError → cli-error.ts (shared leaf) and the
work-agent/execution-policy helpers → work-agent.ts. No package version change.
