---
"@bounded-systems/prx": patch
---

Adopt @bounded-systems/descriptor-kit for STATUS.md + docs/value-props.md generation; retire the bespoke gen-value-props.ts. Byte-parity except the provenance banner; the render layer moves to the shared tool while the value_props.ts catalog + backing helpers are unchanged.
