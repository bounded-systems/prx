---
---

internal: route docs:render / docs:check (and thus prx ci --phase=docs) through the
spec-driven `prx docs` verb instead of the hand-chained sub-scripts. The verb is now
the canonical doc surface. No package version change.
