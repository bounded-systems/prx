---
---

Add the claims-calibration audit instrument (rubric + cohort enumerator + `/claims-audit` command). No-release change: docs, a `scripts/` enumerator, and a test classification only — no published-package surface changes.
