---
---

feat: add the spec-driven `prx schemas` verb (render + check all 48 JSON-Schema
artifacts) and route schemas:export / new schemas:check through it. Also picks up
the derive schemas the old export chain omitted. No package version change.
