---
---

Add 6 verified-publishable packages (disposition, fs, machine-schema, policy, prx-config, verbspec) to the jsr:publish READY set — each passes `jsr publish --dry-run --allow-slow-types`. CI/release config only, no package release.
