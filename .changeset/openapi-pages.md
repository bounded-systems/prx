---
---

Publish `packages/prx/openapi.json` and a zero-build Redoc viewer (`openapi.html`)
on the GitHub Pages site, linked from the index — a live API reference for the
verb surface. Build-tooling + generated-site only; no package release.
