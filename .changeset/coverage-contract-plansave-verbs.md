---
---

internal: raise `contract-verb` (26.5% → 91.7%) and `plan-save-verb` (84.4% →
96.9%) past the per-file 85% floor — drives the contract-trinity registry read
arms (list-all / per-kind list / single-entry / not-found refusals / both
render faces) and the residual plan-save arms (stdin/file mutual exclusion,
`--format=json` envelope). Test-only; another batch toward the per-file 85 gate.
