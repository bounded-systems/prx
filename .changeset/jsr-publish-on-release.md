---
---

JSR release automation: `release.yml` publishes changed packages to JSR on push to main (idempotent, dependency-ordered, tokenless OIDC) via the new `jsr:publish` script — CI/packaging only, no package release.
