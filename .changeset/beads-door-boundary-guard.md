---
---

prx-zbsi (bucket B): add an architecture boundary guard test asserting the beadsd door-gate primitives are referenced only by the sanctioned read sites, and that the host-management / dolt-lifecycle `bd` spawns stay host-only (spawn `bd` directly, never through the read door). Test-only — no release.
