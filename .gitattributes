# Formula/prx.rb is the Homebrew formula generated by the release-binary
# workflow (update-hashes job) — the only Ruby file in this otherwise
# TypeScript/Bun repo. Mark it linguist-generated so GitHub excludes it from
# language detection. This stops CodeQL "default setup" from re-detecting Ruby
# and spinning up a failing, empty `language:ruby` scan configuration.
Formula/prx.rb linguist-generated=true
